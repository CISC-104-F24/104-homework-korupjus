using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello world!");
        Debug.LogWarning("This is a warning.");
        Debug.LogError("Yea its cooked");
        //Author: Justin Korup
        /*
         *Will we only be using C# or will we use different CS languages?
         *My game idea is a card game that has random rewards and levels that are randomly generated each time.
         *I would like to learn about more about random number generators in games.
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
